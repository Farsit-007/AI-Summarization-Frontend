import CreateArticleForm from "@/components/modules/OwnArticle/CreateArticleForm";

const page = () => {
    return (
        <div className="min-h-screen flex items-center justify-center">
            <CreateArticleForm/>
        </div>
    );
};

export default page;